import React from 'react';
import {
} from 'reactstrap';

const Header = () => (
    <div color='dark' className="text-center text-light bg-dark">
        <h1>Proexe</h1>
    </div>
);

export default Header;
